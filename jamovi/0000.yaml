---
name: easyRasch2jmv
title: easyRasch2 for Jamovi
version: 0.5.1
jms: '1.0'
authors:
  - Magnus Johansson
maintainer: Magnus Johansson <pgmj@pm.me>
date: '2026-05-26'
type: R
description: >
  Provides Rasch analysis functions for Jamovi, wrapping analysis logic from the
  easyRasch2 R package. Includes conditional item infit, item-restscore, and
  partial gamma DIF from the iarm package, Yen's Q3 residuals from package mirt,
  and item probability curves (PCM) from eRm. Uses CML estimation from eRm,
  except for the analysis of Q3 residuals, which use MML estimation.
analyses:
  - title: Item-Restscore Correlations
    name: itemrestscore
    ns: easyRasch2jmv
    menuGroup: easyRasch2jmv
    menuTitle: Item-Restscore Correlations
    category: analyses
    description: |
      Computes observed and model-expected item-restscore correlations using
      Goodman-Kruskal's gamma (via the iarm package). Fits a Rasch model
      (dichotomous data) or Partial Credit Model (polytomous data) using eRm,
      then reports observed vs. expected restscore correlations, the signed
      difference (observed minus expected; positive = over-discrimination /
      often LD, negative = under-discrimination / often noise),
      adjusted p-values, and item locations.
  - title: Q3 Residual Correlation Matrix
    name: locdepq3
    ns: easyRasch2jmv
    menuGroup: easyRasch2jmv
    menuTitle: Q3 Residual Correlation Matrix
    category: analyses
    description: |
      Computes Yen's Q3 residual correlations between item pairs for local
      dependence assessment in Rasch models (fitted via mirt). Optionally
      runs a parametric bootstrap simulation to determine a simulation-based
      cutoff value. Single-core sequential processing is used by default.
  - title: Conditional Item Infit
    name: iteminfit
    ns: easyRasch2jmv
    menuGroup: easyRasch2jmv
    menuTitle: Conditional Item Infit
    category: analyses
    description: |
      Computes conditional infit MSQ statistics for each item using
      iarm::out_infit(). Optionally determines simulation-based cutoff
      values via parametric bootstrap and visualises the results.
  - title: Andersen LR-test DIF
    name: lrdif
    ns: easyRasch2jmv
    menuGroup: easyRasch2jmv
    menuTitle: Andersen LR-test DIF
    category: analyses
    description: |-
      Splits a Rasch model by an external categorical grouping variable
      using `eRm::LRtest()` (Andersen's likelihood-ratio test) and reports
      per-group item locations (or per-group threshold locations) together
      with their standard errors. The dichotomous Rasch Model (RM) is
      fitted when the data are 0/1; the Partial Credit Model (PCM)
      otherwise -- chosen automatically.
  - title: Partial Gamma DIF
    name: partgamdif
    ns: easyRasch2jmv
    menuGroup: easyRasch2jmv
    menuTitle: Partial Gamma DIF
    category: analyses
    description: |
      Computes partial gamma coefficients for Differential Item Functioning
      (DIF) using iarm::partgam_DIF(). Optionally determines simulation-based
      cutoff values via parametric bootstrap and visualises the results.
  - title: Item Probability Curves
    name: iccplot
    ns: easyRasch2jmv
    menuGroup: easyRasch2jmv
    menuTitle: Item Probability Curves
    category: analyses
    description: |
      Fits a Rasch model using eRm (CML) and displays item characteristic
      curve (ICC) trace plots showing the probability of each response
      category as a function of the latent trait (theta).
  - title: Dynamic CFA Fit-Index Cutoffs
    name: cfacutoff
    ns: easyRasch2jmv
    menuGroup: easyRasch2jmv
    menuTitle: Dynamic CFA Fit-Index Cutoffs
    category: analyses
    description: |-
      Tests whether the observed one-factor categorical-CFA fit
      indices (CFI, RMSEA, SRMR) are consistent with the data being
      generated by a unidimensional Rasch / Partial Credit Model.
  - title: Targeting Plot
    name: targeting
    ns: easyRasch2jmv
    menuGroup: easyRasch2jmv
    menuTitle: Targeting Plot
    category: analyses
    description: |
      Person-item targeting plot (Wright Map) showing back-to-back histograms
      of person and item threshold locations, plus a dot plot of individual
      item thresholds. Uses CML estimation via eRm.
  - title: Bootstrap Item-Restscore
    name: bootrestscore
    ns: easyRasch2jmv
    menuGroup: easyRasch2jmv
    menuTitle: Bootstrap Item-Restscore
    category: analyses
    description: >
      Non-parametric bootstrap of item-restscore fit using
      iarm::item_restscore().

      For each iteration a sample of size `samplesize` is drawn from the data

      with replacement, the appropriate Rasch model (eRm::RM for dichotomous,

      psychotools::pcmodel for polytomous) is refitted, and items are classified

      as overfit, underfit, or no misfit based on the BH-adjusted p-value

      (< .05) and the sign of (expected - observed). Useful with large samples

      where the asymptotic test can flag items that are not practically

      misfitting.
  - title: Conditional Item Infit (Multiple Imputation)
    name: iteminfitmi
    ns: easyRasch2jmv
    category: analyses
    menuGroup: easyRasch2jmv
    menuTitle: Conditional Item Infit (Multiple Imputation)
    description: |-
      Computes pooled conditional infit MSQ statistics for items with missing
      responses. Internally runs mice::mice() to generate `m` imputed datasets,
      fits a Rasch model (eRm::RM for dichotomous, eRm::PCM for polytomous) on
      each, computes conditional infit MSQ via iarm::out_infit(), and pools
      per-item estimates and standard errors using Rubin's rules.
  - title: Reliability
    name: reliability
    ns: easyRasch2jmv
    menuGroup: easyRasch2jmv
    menuTitle: Reliability
    category: analyses
    description: |-
      Reports four reliability indices for a Rasch / partial credit model:
      Cronbach's alpha (closed-form), the Person Separation Index (PSI) via
      eRm::SepRel(), empirical reliability via mirt::empirical_rxx(), and
      Relative Measurement Uncertainty (RMU) computed from mirt plausible
      values using the Bignardi, Kievit & Bürkner (2025) split-half
      correlation method.
  - title: Sum Score to Logit Transformation
    name: scorese
    ns: easyRasch2jmv
    menuGroup: easyRasch2jmv
    menuTitle: Sum Score to Logit Transformation
    category: analyses
    description: |-
      Maps each possible raw sum score to a person-location estimate (in
      logits) and its standard error. Useful when reporting a scale's
      measurement properties or converting raw totals to interval-scaled
      scores.
  - title: PCA of Standardized Rasch Residuals
    name: residualpca
    ns: easyRasch2jmv
    category: analyses
    menuGroup: easyRasch2jmv
    menuTitle: PCA of Standardized Rasch Residuals
    description: |-
      Fits a Rasch model (RM for dichotomous data, PCM for polytomous --
      chosen automatically) and runs an unrotated principal-component
      analysis on the standardized residuals via `eRm::itemfit()` and
      `stats::prcomp()`. Reports the largest eigenvalues, the proportion of
      unexplained variance, and a loadings-vs-location plot for inspecting
      the first residual contrast.
datasets:
  - name: pcmdat2
    path: eRm_pcmdat2.csv
    description: Polytomous dataset from eRm
  - name: raschdat3
    path: eRm_raschdat3.csv
    description: Dichotomous dataset from eRm
  - name: pcmdat2dif
    path: eRm_pcmdat2dif.csv
    description: Polytomous dataset from eRm with added DIF variable
usesNative: false
minApp: 1.0.8

...
